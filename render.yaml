services:
  - type: web
    name: skill-swap-frontend
    runtime: node
    rootDir: frontend
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production